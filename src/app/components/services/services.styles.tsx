import styled from "styled-components";

export const ServicesNavButton = styled.span`
  display: flex;
  align-items: center;
  gap: 4px;
  cursor: pointer;

  svg:nth-child(2) {
    display: none;
  }
`